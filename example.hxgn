void otherFunc(int8 var1, int16 var2)
{
	int32 var3 = var1 + var2;
}

// Every hexagn program needs a main function that returns an int8
int8 main()
{
	// Variable definition syntax
	int8 a = 5;
	int32 b = 10000;
	int16 c = a * (b - 5);

	// Nested functions are supported (for now)
	void inner(int16 arg)
	{
		int32 uselessVariable = arg;
	}

	// Function calls
	inner(c);
	otherFunc(a, c);
}
